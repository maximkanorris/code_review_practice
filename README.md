# code_review_practice
def calc_disc(price,age): #тут бы описание функции
  if age>18: #магическое число 18 и не написан проецнт скидки(0.1)
    new_price=price-(price*0,1)
    print("скидка применена")
    return new_price
  else:
  #не обрабатывает случай когда возрат равен 18
p=1000 #плохое имя переменной и маг число
a=int(input(""))
final_price=
